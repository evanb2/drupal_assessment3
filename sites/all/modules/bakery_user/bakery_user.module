<?php

function bakery_user_menu() {
    $items = array();
    $items['welcome'] = array(
        'title' => 'Hi There!',
        'page callback' => 'welcome_new_user',
        'access callback' => TRUE,
        'type' => MENU_NORMAL_ITEM,
    );
    return $items;
}

function welcome_new_user() {
    return '<h1>Welcome! Sign our guestbook!</h1>';
}
